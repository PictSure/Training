training_loc:

resnet:
  type:
  pretrained: 
visnet: 

name: 

paths:
  output:
  dataset:
  train:
  test:
  visnet_weights:

model:
  nheads:
  nlayers:

optimizer:
  lr_initial: # lr_max
  lr_target: # lr_min
  epochs:
  weight_decay:
  log_step:
  epsilon:
  acc_steps:

dataloader:
  num_samples:
  batch_size:
  num_classes:
  num_images:
  train_ratio: 
  test_ratio:
  num_workers:
