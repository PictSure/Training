training_loc: "cluster"

paths:
  output: /netscratch/schiesser/embed_then_cat/runs/
  dataset: /ds/images/ILSVRC12/train.msgpack

resnet:

model:
  nheads: 4
  nlayers: 8

optimizer:
  lr: 0.001
  lr_initial: 
  lr_target:
  epochs: 40
  weight_decay: 0.00001
  log_step: 500
  epsilon: 
  acc_steps: 1

dataloader:
  num_samples: 10000
  batch_size: 16
  num_classes: 4
  num_images: 